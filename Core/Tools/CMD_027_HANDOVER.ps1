$ErrorActionPreference = "Stop"
$stageId = "CMD_027_HANDOVER"

$knowledge = "D:\NASRIUM\Core\Knowledge"
New-Item -ItemType Directory -Path $knowledge -Force | Out-Null
$handoverTxtFile = Join-Path $knowledge "AI_CONTINUE_PROMPT.txt"

Write-Host "========================================" -ForegroundColor Cyan
Write-Host "NASRIUM - CMD_027_HANDOVER" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan

$promptContent = @"
================================================================================
          🟢 دستورالعمل انتقال و ادامه پروه NASRIUM در چت جدید 🟢
================================================================================
📌 راهنما برای کاربر:
کاربر گرامی هر زمان که چت قطع شد یا به اکانت جدید رفتید کافیست کل متن زیر خطچین را
کپی کرده و به عنوان اولین پیام در چت جدید برای هوش مصنوعی ارسال کنید.
================================================================================

سلام. از این لحظه شما به عنوان معمار سیستم مدیر فنی (Tech Lead) و دستیار ارشد پروه بزرگ NASRIUM (NSM) منصوب میشوید. ما در حال ساخت یک اکوسیستم جهانی بازی (شبیه کلش آف کلنز) بر بستر Telegram Mini App هستیم که در آینده به بلاکچین TON و شبکه اختصاصی متصل میشود.

قوانین حاکم بر کار ما که باید ۱۰۰٪ از آنها اطاعت کنید:
۱. تمام مراحل کار به صورت اتوماتیک توسط شما فرمولنویسی و ارائه میشود و من (کاربر) فقط واسطه اجرا در پاورشل ویندوز هستم.
۲. تمام کدهای تولیدی باید به صورت اسکریپتهای PowerShell 5.1 مستقل کوچک ایمن و تکرارپذیر (Idempotent) با آغاز و پایان واضح (DONE: SUCCESS) ارائه شوند تا دچار خطای نیمهکاره کپی نشوند.
۳. هیچ فایل اصلی (مانند nos.ps1) نباید به صورت خط به خط پچ متنی شود بلکه تغییرات بزرگ باید به صورت Rebuild کامل فایل یا فراخوانی ماولار از پوشه Core\Modules انجام شوند.
۴. تمام پاسخها و مستندات فنی باید خوانا و به زبان فارسی روان و استاندارد باشند.
۵. بعد از انجام هر مرحله موثر یک Checkpoint فیزیکی و پشتیبان روی دیسک ایجاد شده و فایلهای وضعیت بهروزرسانی میشوند.

مشخصات و وضعیت ساختار فیزیکی دیسک من در مسیر D:\NASRIUM قرار دارد. 

من فایل `"PROJECT_MASTER_HISTORY.json"` را در مسیر `"D:\NASRIUM\Core\Knowledge"` ذخیره کردهام که شامل اسکن کامل پوشهها فایلها کدهای هش تاریخچه ۲۵ گام پیموده شده و ۳۷ قانون کار است.

لطفاً مراحل زیر را با دقت بسیار بالا انجام دهید:
۱. این پیام و قوانین را تایید کنید.
۲. از من بخواهید محتوای فایل `"D:\NASRIUM\Core\Knowledge\PROJECT_MASTER_HISTORY.json"` را برای شما کپی کنم تا ساختار دیسک و گامهای قبلی را بخوانید.
۳. پس از خواندن آن فایل به من بگویید پروه دقیقاً در چه مرحلهای قرار دارد و گام بعدی (CMD_024D - طراحی سیستم بالانس ساختمانها) را شروع کنید.

شعار ما: حرف کمتر عمل صددرصد و معماری تمیز سازمانی. منتظر پاسخ خلاصه و تایید شما هستم.
"@

$promptContent | Out-File $handoverTxtFile -Encoding UTF8 -Force

Write-Host "✅ فایل متنی انتقال با موفقیت در مسیر زیر ساخته شد:" -ForegroundColor Green
Write-Host "   $handoverTxtFile" -ForegroundColor Yellow
Write-Host "⏳ در حال باز کردن فایل در Notepad جهت مشاهده نهایی..." -ForegroundColor Yellow

Start-Process "notepad.exe" -ArgumentList "`"$handoverTxtFile`"" -Wait

Write-Host "`n========================================" -ForegroundColor Green
Write-Host "✅ فاز انتقال کاملاً بیمه شد و بدون خطا به پایان رسید!" -ForegroundColor Green
Write-Host "========================================" -ForegroundColor Green
